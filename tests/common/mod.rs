
//This file will NOT be compiled as a separate crate when `cargo test` is run. This is because it
// is in a subdirectory. These subdirectories can be used for setup, teardown etc. type functions.

pub fn setup_test() {
    //Stuff
}